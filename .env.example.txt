# JurisAI — Environment Variable Template
# ============================================================================
# Copy this file to .env.local and fill in real values for local
# development. .env.local is gitignored and must never be committed.
#
# This file documents variable NAMES only — no real values, keys, or
# secrets belong here. Source of truth for what's actually required:
#   - src/core/config/env.ts          (Public / client-safe)
#   - src/core/config/env.server.ts   (Server-only, core)
#   - src/core/config/env.billing.ts  (Server-only, billing/Cashfree)
# ============================================================================

# ----------------------------------------------------------------------------
# Public
# Safe to expose to the browser (NEXT_PUBLIC_ prefix required by Next.js).
# Validated in src/core/config/env.ts
# ----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Optional — defaults to "openai" if unset. One of: openai, gemini
NEXT_PUBLIC_AI_DEFAULT_PROVIDER=

# ----------------------------------------------------------------------------
# Server-only
# Never exposed to the client bundle. Validated eagerly at app boot in
# src/core/config/env.server.ts — a missing/invalid value here prevents
# the app from starting.
# ----------------------------------------------------------------------------
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
GOOGLE_GENERATIVE_AI_API_KEY=
# Full JSON contents of a Google Cloud service account key (client_email,
# private_key, project_id required; other standard key fields allowed).
GOOGLE_CLOUD_VISION_SERVICE_ACCOUNT_KEY=
GOOGLE_CLOUD_VISION_STAGING_BUCKET=
# Arbitrary secret string you generate yourself; must match the
# Authorization: Bearer <CRON_SECRET> header the cron scheduler sends.
CRON_SECRET=

# ----------------------------------------------------------------------------
# Billing / Cashfree
# Server-only. Validated lazily, only when billing code is actually
# invoked (src/core/config/env.billing.ts) — NOT required for core app
# boot, sign-in, documents, cases, or any non-billing functionality.
# ----------------------------------------------------------------------------
CASHFREE_CLIENT_ID=
CASHFREE_CLIENT_SECRET=
# Must be exactly "sandbox" or "production"
CASHFREE_ENVIRONMENT=
CASHFREE_WEBHOOK_SECRET=
